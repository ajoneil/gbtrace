[profile]
name = "blargg-ppu"
description = "Blargg test suite with full state, pixel output, and PPU internals."
trigger = "tcycle"

[fields]
cpu = "registers"
ppu = "all"
timer = true
interrupt = true
serial = true
