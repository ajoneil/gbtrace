[profile]
name = "blargg"
description = "CPU fields, timer registers, and pixel output for blargg tests."
trigger = "tcycle"

[fields]
cpu = "registers"
ppu = ["registers", "output"]
timer = true
interrupt = true
