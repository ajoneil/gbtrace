[profile]
name = "dmg_sound"
description = "APU register capture for blargg dmg_sound tests."
trigger = "tcycle"

[fields]
cpu = "registers"
apu = ["registers", "internal", "writes"]
timer = true
interrupt = true

[fields.memory]
test_status = "A000"
test_sig1   = "A001"
