[profile]
name = "dmg_sound"
description = "APU register capture for blargg dmg_sound tests."
trigger = "tcycle"

[fields]
cpu = "registers"
ppu = "output"
apu = ["registers", "internal", "writes"]
timer = true
interrupt = true

