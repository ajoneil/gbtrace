[profile]
name = "gambatte"
description = "Full dump for Gambatte test suite. PPU-heavy tests need full state."
trigger = "tcycle"

[fields]
cpu = "registers"
ppu = "all"
timer = true
interrupt = true
